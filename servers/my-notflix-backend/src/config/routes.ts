export const API_PREFIX = "/api" as const;
export const API_VERSION = "/v1" as const;

export const ROUTE_SEGMENT = {
  ROOT: "/",
  ID_PARAM: "/:id",

  AUTH: "/auth",
  LOGIN: "/login",
  REGISTER: "/register",

  USERS: "/users",
};
